### Converting manifests from JSON to YAML

A simple script to help convert JSON manifests to YAML.

This depends on [PyYAML](https://pypi.org/project/PyYAML/).

Usage:

```sh
./flatpak-json2yaml.py /path/to/example.json --output=example.yml
```
